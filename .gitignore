*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.kotlin/
/log.txt
/full_log.txt
scratch_*
.claude/
.agents/

# Stray local artifacts pulled/created during on-device debugging - not source, never commit.
downloads.db
gallerydl_database
test_cookies.txt
/gallery-dl/

# Local release-signing keystore (app/build.gradle.kts's "release" signingConfig) - a signing key
# is the one thing here that actually matters to keep private/local, unlike the placeholder
# store/key passwords next to it in that file.
/release.keystore
